-- Edited Yeet Gui by Grok
-- Added: Power Fling + Touch Fling Toggle

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local StarterGui = game:GetService("StarterGui")

local lp = Players.LocalPlayer
local touchFlingEnabled = false
local touchConnection = nil

local function gplr(String)
	local Found = {}
	local strl = String:lower()
	if strl == "all" then
		for _, v in pairs(Players:GetPlayers()) do table.insert(Found, v) end
	elseif strl == "others" then
		for _, v in pairs(Players:GetPlayers()) do
			if v ~= lp then table.insert(Found, v) end
		end
	elseif strl == "me" then
		table.insert(Found, lp)
	else
		for _, v in pairs(Players:GetPlayers()) do
			if v.Name:lower():sub(1, #String) == strl then
				table.insert(Found, v)
			end
		end
	end
	return Found
end

local function notif(title, text, dur)
	StarterGui:SetCore("SendNotification", {
		Title = title or "Yeet Gui",
		Text = text,
		Icon = "rbxassetid://2005276185",
		Duration = dur or 5
	})
end

-- GUI (sama seperti aslinya, tapi ditambah tombol baru)
local h = Instance.new("ScreenGui")
h.Name = "YeetGui"
h.Parent = game:GetService("CoreGui")
h.ResetOnSpawn = false

local Main = Instance.new("ImageLabel")
Main.Name = "Main"
Main.Parent = h
Main.Active = true
Main.Draggable = true
Main.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Main.BorderSizePixel = 0
Main.Position = UDim2.new(0.17, 0, 0.46, 0)
Main.Size = UDim2.new(0, 454, 0, 280)  -- diperbesar sedikit biar muat tombol baru
Main.Image = "rbxassetid://2005276185"

local Top = Instance.new("Frame")
Top.Name = "Top"
Top.Parent = Main
Top.BackgroundColor3 = Color3.fromRGB(57, 57, 57)
Top.BorderSizePixel = 0
Top.Size = UDim2.new(1, 0, 0, 44)

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = Top
Title.BackgroundColor3 = Color3.fromRGB(49, 49, 49)
Title.BorderSizePixel = 0
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Position = UDim2.new(0, 0, 0.3, 0)
Title.Font = Enum.Font.SourceSans
Title.Text = "FE Yeet Gui (trollface edition) - Edited"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextScaled = true

local TextBox = Instance.new("TextBox")
TextBox.Parent = Main
TextBox.BackgroundColor3 = Color3.fromRGB(49, 49, 49)
TextBox.BorderSizePixel = 0
TextBox.Position = UDim2.new(0.07, 0, 0.22, 0)
TextBox.Size = UDim2.new(0, 388, 0, 50)
TextBox.Font = Enum.Font.SourceSans
TextBox.PlaceholderText = "Who do i destroy (can be shortened)"
TextBox.Text = ""
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextScaled = true

-- Tombol Normal Fling
local NormalButton = Instance.new("TextButton")
NormalButton.Parent = Main
NormalButton.BackgroundColor3 = Color3.fromRGB(49, 49, 49)
NormalButton.BorderSizePixel = 0
NormalButton.Position = UDim2.new(0.07, 0, 0.45, 0)
NormalButton.Size = UDim2.new(0, 180, 0, 40)
NormalButton.Font = Enum.Font.SourceSans
NormalButton.Text = "Normal Fling"
NormalButton.TextColor3 = Color3.fromRGB(255, 255, 255)
NormalButton.TextScaled = true

-- Tombol Power Fling
local PowerButton = Instance.new("TextButton")
PowerButton.Parent = Main
PowerButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
PowerButton.BorderSizePixel = 0
PowerButton.Position = UDim2.new(0.52, 0, 0.45, 0)
PowerButton.Size = UDim2.new(0, 180, 0, 40)
PowerButton.Font = Enum.Font.SourceSans
PowerButton.Text = "Power Fling"
PowerButton.TextColor3 = Color3.fromRGB(255, 255, 255)
PowerButton.TextScaled = true

-- Tombol Touch Fling Toggle
local TouchButton = Instance.new("TextButton")
TouchButton.Parent = Main
TouchButton.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
TouchButton.BorderSizePixel = 0
TouchButton.Position = UDim2.new(0.07, 0, 0.65, 0)
TouchButton.Size = UDim2.new(0, 388, 0, 40)
TouchButton.Font = Enum.Font.SourceSans
TouchButton.Text = "Touch Fling: OFF"
TouchButton.TextColor3 = Color3.fromRGB(255, 255, 255)
TouchButton.TextScaled = true

-- Fungsi Fling Normal (seperti aslinya)
local function normalFling(target)
	local root = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
	if not root or not target.Character or not target.Character:FindFirstChild("HumanoidRootPart") then return end
	
	local thrust = Instance.new("BodyThrust")
	thrust.Force = Vector3.new(9999, 9999, 9999)
	thrust.Parent = root
	thrust.Name = "YeetForce"
	
	spawn(function()
		for i = 1, 60 do  -- loop sebentar biar lebih stabil
			if not target.Character or not target.Character:FindFirstChild("HumanoidRootPart") then break end
			root.CFrame = target.Character.HumanoidRootPart.CFrame
			thrust.Location = target.Character.HumanoidRootPart.Position
			RunService.Heartbeat:Wait()
		end
		thrust:Destroy()
	end)
end

-- Fungsi Power Fling (lebih kuat + sedikit prediksi)
local function powerFling(target)
	local root = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
	local tRoot = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
	if not root or not tRoot then return end
	
	local bv = Instance.new("BodyVelocity")
	bv.Name = "PowerFling"
	bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
	bv.Velocity = (tRoot.Position - root.Position).Unit * 150 + Vector3.new(0, 50, 0)  -- tambah sedikit ke atas
	bv.Parent = root
	
	spawn(function()
		for i = 1, 40 do
			if not tRoot.Parent then break end
			root.CFrame = tRoot.CFrame * CFrame.new(0, 0, -1.5)
			bv.Velocity = (tRoot.Velocity * 1.2) + Vector3.new(0, 80, 0)
			RunService.Heartbeat:Wait()
		end
		if bv then bv:Destroy() end
	end)
	
	notif("Power Fling", "Yeeting " .. target.Name .. " dengan kekuatan tinggi!", 3)
end

-- Touch Fling Logic
local function enableTouchFling()
	if touchConnection then return end
	touchFlingEnabled = true
	TouchButton.Text = "Touch Fling: ON"
	TouchButton.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
	
	touchConnection = lp.Character:WaitForChild("HumanoidRootPart").Touched:Connect(function(hit)
		local char = hit.Parent
		local plr = Players:GetPlayerFromCharacter(char)
		if plr and plr ~= lp then
			local tRoot = char:FindFirstChild("HumanoidRootPart")
			if tRoot then
				local bv = Instance.new("BodyVelocity")
				bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
				bv.Velocity = lp.Character.HumanoidRootPart.Velocity * 2 + Vector3.new(0, 100, 0)
				bv.Parent = tRoot
				game.Debris:AddItem(bv, 0.6)
			end
		end
	end)
end

local function disableTouchFling()
	touchFlingEnabled = false
	TouchButton.Text = "Touch Fling: OFF"
	TouchButton.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
	if touchConnection then
		touchConnection:Disconnect()
		touchConnection = nil
	end
end

-- Event Tombol
NormalButton.MouseButton1Click:Connect(function()
	local targets = gplr(TextBox.Text)
	if targets[1] then
		normalFling(targets[1])
		notif("Normal Fling", "Yeeting " .. targets[1].Name, 3)
	else
		notif("Error", "Player tidak ditemukan!", 3)
	end
end)

PowerButton.MouseButton1Click:Connect(function()
	local targets = gplr(TextBox.Text)
	if targets[1] then
		powerFling(targets[1])
	else
		notif("Error", "Player tidak ditemukan!", 3)
	end
end)

TouchButton.MouseButton1Click:Connect(function()
	if touchFlingEnabled then
		disableTouchFling()
	else
		enableTouchFling()
	end
end)

notif("Yeet Gui Loaded", "Normal Fling + Power Fling + Touch Fling ditambahkan!", 6)
print("Yeet Gui Edited - Power Fling & Touch Fling ON")
